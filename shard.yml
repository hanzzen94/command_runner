name: command_runner
version: 0.1.0

authors:
  - Håvard Lauve Hansen <havard.hansen@posteo.net>

targets:
  command_runner:
    main: src/agent/main.cr
  central_server:
    main: src/server/main.cr

dependencies:
  amqp-client:
    github: cloudamqp/amqp-client.cr
  pg:
    github: will/crystal-pg

crystal: '>= 1.21.0'

license: MIT
